<ui:FluentWindow x:Class="DisplayRefreshRate.ShortcutEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Edit Shortcut"
        Height="280" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewKeyUp="Window_PreviewKeyUp">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="Edit Shortcut" ShowMaximize="False" ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Keyboard24"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="24,12,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ui:TextBlock Text="Press the desired shortcut keys" 
                          FontTypography="Body" 
                          Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                          HorizontalAlignment="Center"/>

            <Border Grid.Row="1" 
                    Background="{ui:ThemeResource ControlFillColorDefaultBrush}" 
                    CornerRadius="8" 
                    Padding="16"
                    Margin="0,16"
                    VerticalAlignment="Center">
                <ui:TextBlock x:Name="ShortcutText" 
                              Text="None" 
                              FontTypography="Title" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel" Click="Cancel_Click" Appearance="Secondary" Margin="0,0,8,0"/>
                <ui:Button x:Name="SaveButton" Content="Save" Click="Save_Click" Appearance="Primary" IsEnabled="False"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
